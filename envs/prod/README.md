# KAMBRIQ - Environnement PROD

**⚠️ ATTENTION :** Cette configuration utilise encore les modules V1 (Lambda + OpenNext + NestJS).

**Status :** À migrer vers V2

**Action requise :**
1. Créer `envs/prod-v2/` avec la configuration V2 (comme `envs/dev-v2/`)
2. Migrer l'infrastructure prod vers V2
3. Supprimer cette configuration V1 une fois la migration complétée

**Configuration V2 :** Voir `envs/dev-v2/` pour référence.

